# CropFusion Dataset Management System — runtime dependencies
# Python >= 3.12
#
# Core:
kagglehub>=0.3.0            # automatic Kaggle dataset download
pandas>=2.2                 # CSV profiling / loading
numpy>=1.26                 # raster array handling
rasterio>=1.3               # GeoTIFF header + windowed reads (GDAL)
pydantic>=2.0               # validated configuration models
PyYAML>=6.0                 # YAML configuration files

# Optional — metadata export to Parquet:
pyarrow>=15.0

# Development / testing:
pytest>=8.0                 # test runner
