# CropFusion Kaggle training environment (R2.1 infrastructure).
#
# Mirrors the minimum training runtime used by the Kaggle notebooks in
# training/kaggle/notebooks/. The authoritative dependency set lives in the
# root requirements.txt.
#
# Infra note: tensorflow / opencv-python / GDAL are optional and only probed
# (reported as missing) by the Environment Manager — they are not required to
# run the infrastructure phase.
pandas>=2.2
numpy>=1.26
torch>=2.3
scikit-learn>=1.5
rasterio>=1.3
timm>=1.0
pydantic>=2.5
PyYAML>=6.0
psutil>=5.9
kagglehub>=0.3
