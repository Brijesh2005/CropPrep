# CropFusion - development / CI tooling.
# Install after requirements.txt:
#   pip install -r requirements-dev.txt

pytest==9.1.1
pytest-cov==7.1.0
pytest-benchmark==5.2.3
coverage==7.15.2
fakeredis==2.37.0

# Linters / static analysis
ruff>=0.6,<1
mypy>=1.10
bandit==1.9.4
safety==3.8.1

# Frontend tooling is managed by npm (see application/frontend/package.json).
