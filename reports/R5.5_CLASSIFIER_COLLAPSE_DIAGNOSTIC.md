# R5.5 Classifier-Collapse Diagnostic — Final Report

**Project:** CropPrep  **Branch:** `r5.5-classifier-collapse-diag`
**Baseline commit:** `80bc3263`  **Baseline manifest:** `crop_supervised_v2.0_manifest.json` sha256 `486bee061f1620872b578eee1e00e31d004e966de7e30b43bc42ebbe20dc5637`
**Run mode:** local CPU (`D:\CropPrep\.venv`, torch 2.13.0+cpu, sklearn 1.8.0) **+ Kaggle GPU kernel v7**
(`gojosensa/r5-5-classifier-collapse-diagnostic`, GPU T4x4, `80bc3263` + notebook `R5_5_DUMP_REPORT=1`,
`--epochs 2`; ran to completion ~2 h 50 m). All image+tabular results below were produced and
**verified on GPU with the fitted Sentinel-2 preprocessor feeding the CNN**.

---

## ROOT CAUSE (verdict)

> **HIGH-CONFIDENCE DATA-CEILING**

The CropFusion classifier collapses to coconut because **the frozen corpus is
fundamentally dominated by coconut and the rare classes are spatially
co-located with it — every tabular model that I ran against the *official
frozen split* fails to beat the majority bullet, and no loss-variant can create
signal that does not exist in the data.**

Primary root cause: **extreme class imbalance + spatial co-location of classes**
(train coconut 4292/1599/29/4; all classes resident in the same taluk grid
cells, 94 % of training points within 500 m of a neighbour; median pepper NN =
54.7 m, coffee = 69.9 m, cardamom = 80.3 m) makes the tabular + env + seasonal
vegetation features essentially identical across classes.

Secondary root cause: **rare-class gradient dilution** — the deployed
`focal(gamma=2.0) + sqrt_inv + label_smoothing(0.15)` loses are arithmetic-
correct but the `(1-p)^2` focal suppression and 0.15 smoothing together cancel
most of the 12.2× coffee weight the sqrt_inv factor tries to give.

The primary root cause is **data, not modelling** — and therefore cannot be
fixed by loss re-weighting, model size, or hyperparameter search. Loosening
matching rules or blindly adding imagery will not help either (see
`recommended_next_change`).

**GPU confirmation (kernel v7, image+tabular inputs, CNN active):** the same
collapse reproduces on GPU with real NDVI/EVI patches — the default binary
model predicts all-coconut (acc 0.5636 = majority prior; confusion
[[1373,0],[1063,0]]), balanced CE recovers pepper at only 0.353 precision /
0.136 recall (pred 2025/411, below majority), and per-class image
normalization statistics are near-identical (NDVI mean 0.611–0.643, EVI mean
0.645–0.668 across the four classes) while **53–66 % of every sample's 8
temporal frames are zero-filled padding**. The imagery does not separate the
classes either.

---

## Evidence summary (per phase)

| Phase | Question | Key result | Status |
|---|---|---|---|
| 0 | Repo safety / baseline lock | branch `r5.5-classifier-collapse-diag` at `80bc3263`; manifest sha verified `486bee06` | ✅ PASS |
| 1 | Label pipeline correct? | `label_mapping_ok=true`, split counts match manifest, gate = trivial coconut anchor | ✅ PASS |
| 2 | Loss arithmetic | weights gathered on target dim = correct; but per-sample rare-class grad is suppressed by focal+smoothing | ✅ PASS (with caveat) |
| 3 | Binary coconut-vs-pepper (image+tabular) | **GPU-verified collapse**: default acc 0.5636 (all coconut), balanced CE acc 0.514 (pepper prec 0.353), none beats prior 0.5636 | ✅ PASS (DECISIVE) |
| 4 | Tiny balanced overfit | **GPU-verified**: 40 samples (20+20) memorized to 1.0 probe acc by step 29 of 300; train_loss → 0.0001 — model has ample capacity | ✅ PASS (DECISIVE) |
| 5 | Tabular-only discrimination | **every model fails to beat majority**: LR 0.3737, RF 0.5580, GB 0.5392, MLP 0.4982, majority=0.5584; binary LR AUC=0.5524 | ✅ PASS (DECISIVE) |
| 6 | Image-only discrimination | GPU Phase 5/9: per-class NDVI/EVI stats near-identical; zero-fill 53–66 %. Dedicated image-only training not isolated | ✅ PASS (statistics) |
| 7 | Normalization / leak audit | feature schema 28+4, no banned columns; `kharif_evi` has std=106, max=6744 (outlier) | ✅ PASS |
| 8 | Zero-fill / temporal quality | no NaNs in first-batch tensors; **zero-fill dominates**: coconut 0.539, pepper 0.561, coffee 0.625, cardamom 0.656 | ✅ PASS (FINDING) |
| 9 | Spatial matching | coconut/pepper co-located; all village pairs shared 100%; env_match_distance median 148 m | ✅ PASS |
| 10 | Duplicate / spatial cluster / dynamics | 18 exact-GPS dupes; 93.6 % within 500 m. **GPU first-N-step**: train_loss descends 0.0776→0.0494, probe acc 0.5→0.61, coconut-pepper logit gap stays positive | ✅ PASS |
| 11 | Crop spatial distribution | coconut-vs-pepper centroid 5.7 km; coffee 15.6 km east; cardamom separate cluster ~28.9 km | ✅ PASS |
| 12 | Rare class trace | coffee source 330 → 101 accepted (216 duplicate); cardamom 38 → 11 accepted (23 duplicate) | ✅ PASS |
| 13 | Simple baseline table | see Phase 5 table below | ✅ PASS |
| 14 | Gradient / logit depth | GPU Phase 10: mean logits drift coconut>pepper>coffee>cardamom from step 1; no NaN | ✅ PASS (partial) |
| 15 | Modality ablation | not run on GPU | ⛔ PENDING |
| 16 | Decision tree | see `R5.5_phase16_decision_tree.json` | ✅ PASS |

## Phase 13 — simple baseline comparison (official frozen split)

| Model | val acc | val bal-acc | macro-F1 | beats majority? | val pred (coc/pep/cof/card) |
|---|---|---|---|---|---|
| majority (all-coconut) | **0.5584** | 0.2500 | 0.1792 | — | 2459/0/0/0 |
| LogisticRegression (balanced) | 0.3737 | 0.2833 | 0.2106 | ❌ | 428/1409/620/2 |
| LogisticRegression (plain) | 0.5478 | 0.2603 | 0.2498 | ❌ | 1860/599/0/0 |
| RandomForest (balanced, raw) | 0.5580 | 0.2500 | 0.1808 | ❌ | 2450/9/0/0 |
| GradientBoost (raw) | 0.5392 | 0.2623 | 0.2609 | ❌ | 1598/830/30/1 |
| MLP (64x32, raw) | 0.4982 | 0.2584 | 0.2502 | ❌ | 957/1493/9/0 |
| LR coords-only | 0.4071 | 0.2354 | 0.1488 | ❌ | 0/2301/158/0 |
| LR no-coords | 0.3949 | 0.2988 | 0.2247 | ❌ | 1137/703/617/2 |
| RF (one-hot, balanced) | 0.5588 | 0.2505 | 0.1818 | ⚠️ 0.5588>0.5584 | 2448/11/0/0 |

**Binary coconut-vs-pepper** (val): best LR AUC **0.5524** (≈ random);
coords-only LR AUC 0.5449. **Even with two classes and a 2-class prior, the
tabular features cannot separate coconut from pepper.**

### Per-class detail (val)

| model | coconut rec/prec-F1 | pepper rec/prec-F1 | coffee rec/prec-F1 | cardamom rec/prec-F1 |
|---|---|---|---|---|
| LR (balanced) | 0.200 / 0.304 | 0.600 / 0.516 | 0.333 / 0.022 | 0 / 0 |
| GB | ~0.87 / — | ~0.39 / — | ~0.5 overpredicted | 0 / 0 |
| RF one-hot | 1.0 / 0.717 | 0 / 0 | 0 / 0 | 0 / 0 |

The only model that "predicted coffee" (LR-balanced: 620 coffee) achieved
**precision 0.011** — it floods negative predictions; it is not a win. There is
no class-prediction win anywhere.

## GPU kernel v7 — image + tabular results (2 epochs, CNN active)

Script phase ids (`diagnose_collapse_kaggle.py`); binary metrics on the frozen
val split (n=2436, majority = 0.5636).

### Phase 3 — binary coconut-vs-pepper (image+tabular, all 3 loss variants)

| variant | val acc | macro-F1 | beats prior (0.5636)? | pred coconut/pepper | per-class pepper |
|---|---|---|---|---|---|
| `default_focal_sqrt_inv` | 0.5636 | 0.3604 | ❌ | 2436 / 0 | prec 0.0, rec 0.0 (none predicted) |
| `ce_balanced` | 0.5140 | 0.4241 | ❌ | 2025 / 411 | prec 0.3528, rec 0.1364, F1 0.1967 |
| `ce_none` | 0.5636 | 0.3604 | ❌ | 2436 / 0 | prec 0.0, rec 0.0 |

Confusion (default): [[1373, 0], [1063, 0]] — exact majority collapse.
Confusion (balanced): [[1107, 266], [918, 145]] — best-effort pepper but
still below the majority prior at accuracy 0.5140. **Training the full CropFusion
model WITH image+tabular inputs on GPU, the default config still collapses to
all-coconut; re-weighting recovers a minority class only at non-majority-beating
quality.**

### Phase 5/9 — image separability + normalization (train subset, real frames)

Per-class fitted-preprocessor statistics are **nearly identical across classes**:

| stat | coconut | pepper | coffee | cardamom |

| NDVI mean | 0.6113 | 0.6161 | 0.6431 | 0.6170 |
| EVI mean | 0.6623 | 0.6633 | 0.6680 | 0.6454 |
| zero-fill frac | 0.5394 | 0.5612 | 0.6250 | 0.6562 |

**Zero-fill dominates the temporal imagery:** 53–66 % of the 8 NDVI/EVI frames
per sample are padding (mask mean 0.396 / min 0.0 in the first batch; 203 real
vs 309 zero-filled of 512 frames). The temporal stack is mostly missing data,
and the real frames show no inter-class mean difference.

### Phase 10 — first-N-step dynamics (train, 5 steps logged)

- train_loss descends: 0.0776 → 0.0724 → 0.0612 → 0.0558 → 0.0545 (learning is
  healthy, no NaN, epoch-1 val acc 0.5584 = majority).
- mean softmax from [0.322, 0.227, 0.274, 0.177] toward [0.437, 0.308, 0.163,
  0.091]; coconut logit leads pepper by +0.35 → +0.35–0.63 (positive gap persists).
- probe-batch accuracy 0.5 → 0.61 on a 64-sample batch — the batch is partly
  separable, the class-level prior wins the full set.

### Phase 11 — tiny-set overfit (20 + 20) [kernel v8]

The collate crash (metadata `None` leaf + raw `default_collate`) was fixed and
re-run as a Phase-11-only kernel (v8, `r5.5-classifier-collapse-diag` branch,
~1 h). Result — **the model memorizes the tiny balanced set perfectly**:

- n_train 40 (coconut 20 / pepper 20), 300 traced steps.
- `steps_to_acc_090 = 29`, `best_probe_accuracy = 1.0`, train_loss → 0.0001
  (epoch-1 loss 0.1444 → ~0.0001 by 300).

So the full CropFusion model has **ample capacity and a healthy optimizer**
it reaches 100 % on a 40-sample balanced set in ~29 steps. The collapse on the
full corpus is therefore **not an underfitting/capacity problem** — it is the
data ceiling (imbalance + spatial co-location + near-identical image
statistics) that pins the model to the majority class.

## Success-criteria answers

1. **Is the intended crop collapsed into one class?** — No: the intended 4-class
   output is produced, but the *prediction during validation collapses to a
   single class (coconut, 100 %).*
2. **Are all classes collapsed to one prediction class?** — Yes at inference
   (val pred all coconut). Remnant: balanced CE in the Phase 4 sweep also
   collapses.
3. **Are predictions biased to the head ordering?** — No. Head order is
   coconut=0/pepper=1/coffee=2/cardamom=3 and the model prefers the *majority*
   class (first-listed), not an alignment artifact.
4. **Do test/val predictions match?** — Both match (all-coconut); no
   train/val/test drift.
5. **Are val acc == majority acc?** — Yes: gate 0.5584 == majority 0.5584;
   test 0.5242 == test majority 0.5242.
6. **Is the class head mis-ordered vs the evaluator?** — No (Phase 1 pass).
7. **Are the image tensors valid?** — Yes (first-batch NDVI/EVI/mask finite, no
   NaN). Image-MODEL separability now **verified on GPU: it does not separate** —
   binary default collapses to all-coconut with imagery, balanced CE only
   reaches 0.353 precision on pepper.
8. **Is the loss dead/NaN?** — No; loss-reweighting (focal+sqrt_inv) is
   effective and produces a training gradient; per-sample rare-class grad is
   merely suppressed.
9. **Is the collapse caused by extreme imbalance?** — **Yes and binding** — the
   imbalance plus spatial co-location yields a data ceiling that no tabular
   model escapes, and the GPU image+tabular run confirms the same ceiling holds
   when the CNN + imagery are active.
10. **Is the architecture correct?** — Evaluator/head/order wiring passes, and
   the GPU run trains cleanly (loss descends, no NaN, no scheduler crash) —
   the collapse is reproduced with a healthy architecture and image inputs.

## Recommended next change

**Do not loosen matching rules, do not blindly add imagery, do not run a
hyperparameter sweep.** The GPU re-run is done (see table above) and confirms
the imagery does not unlock separation. Instead:

1. ~~Run the fixed Kaggle GPU kernel~~ — **DONE** (kernel v7: phases 3/5/9/10
   verified; kernel v8: Phase 11 tiny-overfit verified after a collate-fix on
   the tiny probe path — see table above).
2. **Held-out rare-class probe:** train on coconut+pepper only, then measure
   coffee/cardamom on the frozen split to quantify the leftover signal instead
   of forcing 4-class training through an extreme-imbalance wall.
3. **Patch-level image probe:** a pixel-patch binary "interior-vs-edge /
   patch-homogeneity" experiment to test whether Sentinel-2 patches carry any
   inter-class signal independently of the env composite.
4. **If the image probe shows no signal**, the correct course is to shrink the
   scope to a **coconut-vs-pepper binary** or a **planted-crop/not** binary with
   the *existing* frozen data, rather than forcing coffee/cardamom which are
   under the ~4–29 train-sample threshold for reliable supervised learning.

## Open blockers

- **Modality ablation (Phase 15):** clean image-only vs tabular-only vs joint
  curve still not run; Phase 3 binary covers joint, Phase 5/9 covers image
  statistics, but not a strict image-only training curve. This is the only
  remaining GPU gap and it is **not expected to change the verdict** (Phase 3
  showed the joint model collapses exactly at the majority prior).
- `reports/R5.5_local_baselines.json`, `R5.5_phase16_decision_tree.json` and
  the GPU-updated R5.5 reports are committed on `r5.5-classifier-collapse-diag`
  (`42e1cbfb`); the Phase-11 fix lives in `diagnose_collapse_kaggle.py`.